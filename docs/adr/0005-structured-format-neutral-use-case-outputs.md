# Structured format-neutral Use Case outputs

Use Cases in Exito Tools return structured, format-neutral results rather than pre-rendered strings, tables, or terminal views. Interaction surfaces and output presenters transform those results into JSON, future CLI formats, or Bubble Tea views, keeping domain results reusable and preventing one presentation format from shaping the domain model.
