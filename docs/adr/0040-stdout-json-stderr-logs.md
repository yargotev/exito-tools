# Stdout for command output, stderr or files for logs

The Exito Tools CLI Surface keeps machine-readable command output on stdout and sends logs, debug messages, and diagnostics to stderr or log files. This prevents logging from corrupting the JSON contract while still allowing configurable observability through flags or environment variables.
