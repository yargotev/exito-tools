# Capabilities command exposes neutral inventory

`exito capabilities` exposes a machine-readable inventory of neutral Capabilities, including domain, descriptions, schemas, and mappings to CLI Commands and TUI Actions. It should not be only a list of CLI commands, because agents need to understand the underlying capability contract and how each interaction surface exposes it.
