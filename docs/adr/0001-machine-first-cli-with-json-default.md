# Machine-first CLI with JSON by default

Exito Tools exposes a CLI Surface intended for agents and automation, so CLI commands produce stable machine-readable output by default. JSON is the default output format without requiring an explicit flag; other formats such as Markdown, HTML, or Toon may be added later as selectable output formats.
