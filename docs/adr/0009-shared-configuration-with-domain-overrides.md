# Shared configuration with domain overrides

Exito Tools will use shared application configuration for cross-cutting concerns such as profiles, authentication, and base endpoints, with domain-specific overrides only where needed. Both CLI and TUI surfaces read the same configuration so users and agents do not maintain separate setup paths for the same application.
