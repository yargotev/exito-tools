# Explicit domain wiring

Exito Tools wires domains explicitly in the application layer instead of relying on side-effect imports or auto-registration. Explicit wiring makes registered capabilities visible, testable, and easier to configure with domain-specific dependencies.
