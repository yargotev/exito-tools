# Capability audience metadata

Capabilities in Exito Tools may be marked for agents, people, or both through audience and visibility metadata. Agent-only capabilities remain available through machine-oriented surfaces such as CLI discovery and execution, but are not promoted in human-facing TUI navigation or palette flows unless explicitly allowed.
