# Stable capability identifiers

Capabilities in Exito Tools use stable machine-readable identifiers in the form `<domain>.<action>`, with lower-case kebab-case action names when multiple words are needed. IDs should be short when the domain already provides context, explicit when needed to avoid ambiguity, and not renamed casually because agents may depend on them.
