# Hybrid TUI Action discovery

The Exito Tools TUI Surface will use curated Actions for primary navigation while allowing the Command Palette to discover additional suitable Capabilities. Capability metadata controls visibility and audience so technical or agent-only capabilities do not automatically appear in human-facing TUI flows.
