# Context-aware Capability execution

All Exito Tools Capability executions should accept context and support cancellation where possible. The TUI Surface can use this to represent loading, success, structured error, and cancelled states for long-running Actions, while shared Use Cases remain safe for CLI and TUI execution.
