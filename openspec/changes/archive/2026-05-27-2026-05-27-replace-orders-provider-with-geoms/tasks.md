# Tasks

- [x] Replace Orders HTTP getter endpoint and DTOs with GEOMS `findOrders`.
- [x] Enrich `orders.get` with GEOMS `getOrder` details and `findItemsByOrder` food/non-food items.
- [x] Add GEOMS token acquisition and expiry-aware in-memory caching.
- [x] Extend Orders configuration for client credentials and GEOMS credential bundles.
- [x] Preserve `orders.get` and `exito orders get --id` while adding optional `orderType` / `--order-type`.
- [x] Update non-sensitive docs and YAML base URLs.
- [x] Run `make test` before handoff.
