# Structured errors with stable codes

Exito Tools will represent failures as structured errors with stable codes, human-readable messages, structured details, and retryability metadata when applicable. This gives agents a reliable contract for automation while allowing the TUI Surface to present the same failures in a friendlier way for people.
