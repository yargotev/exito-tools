# Ignore local dotenv files

Exito Tools ignores local `.env` and `.env.*` files while explicitly allowing `.env.example` to be committed. This protects local/profile-specific secrets while keeping a safe environment template in version control.
