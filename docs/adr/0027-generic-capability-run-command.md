# Generic capability run command

Exito Tools will support, at least conceptually, a generic command such as `exito run <capability-id>` for agents to execute any registered Capability by its neutral contract. This path should reuse the same Capability execution pipeline as explicit domain commands, avoiding duplicate business logic while giving agents a stable ID-based invocation route.
