# Risk and confirmation metadata

Capabilities in Exito Tools include risk and confirmation metadata so interaction surfaces can handle read-only, safe-write, and destructive operations appropriately. The CLI Surface can require explicit confirmation flags for risky operations, while the TUI Surface can present clear confirmation prompts with impact before execution.
