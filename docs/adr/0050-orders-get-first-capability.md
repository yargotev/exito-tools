# orders.get as the first Orders Capability

The first Orders Capability in Exito Tools is `orders.get`, exposed as `exito orders get --id <order-id>` and as the TUI Action `Get order by ID`. It is read-only, intended for both agents and people, and establishes the initial contract pattern for domain capabilities.
