# Capability versioning

Capabilities in Exito Tools include version metadata, but compatible changes keep the same Capability ID. Incompatible changes get a new Capability ID with a version suffix such as `.v2`, while the previous capability remains available when possible so agents are not broken silently.
