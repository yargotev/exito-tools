# Root command shows brief text help

Running `exito` without arguments shows brief textual help instead of entering the TUI or emitting the standard JSON envelope. Machine-readable discovery belongs in explicit commands such as `exito capabilities`, preserving a safe default for agents while still helping humans discover the CLI and TUI entrypoints.
