# Orders environment variables

The Orders domain in Exito Tools uses `EXITO_ORDERS_TOKEN` for the provider token and `EXITO_ORDERS_BASE_URL` for the provider base URL. Endpoint paths remain in the Orders Domain API Client, while secrets stay in environment variables or non-committed dotenv files.
