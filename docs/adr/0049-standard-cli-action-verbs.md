# Standard CLI action verbs

Exito Tools uses a small standard vocabulary for CLI action names: `get`, `list`, `search`, `validate`, `diagnose`, and mutating verbs such as `create`, `update`, and `delete` only when they truly modify state. Ambiguous alternatives such as `check`, `consult`, `show`, or `fetch` should be avoided unless a specific domain reason exists.
