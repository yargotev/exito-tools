# Profiles represent environment and credentials

A Profile in Exito Tools represents an environment plus credentials, not a store, region, or domain-specific filter. Commands and TUI flows use the default Profile when none is specified; the initial default is `staging`, and users may change which Profile is the default later.
