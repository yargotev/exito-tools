# Committed env example template

Exito Tools includes a committed `.env.example` template listing supported environment variables without real secret values. Developers and agents copy it to `.env` or `.env.<profile>` for local use, keeping actual tokens and sensitive values out of version control.
