# Tasks

- [x] Add Catalog spec delta for `catalog.create-vtex-segment`.
- [x] Implement domain input/result/use case/capability metadata.
- [x] Implement HTTP Sessions adapter with token redaction.
- [x] Wire brand dispatch and app registration.
- [x] Add CLI command with `--confirm` and `--include-cookie`.
- [x] Add capability docs.
- [x] Add unit tests for input validation, request payload/path, token redaction, app inventory, and CLI confirmation behavior.
- [x] Run `make fmt` and `make test`.
