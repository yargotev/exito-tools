# English visible contracts

Exito Tools will use English for visible contracts, including CLI command names, Capability IDs, labels, help text, and user-facing messages. The orders domain is exposed as `orders` rather than `pedidos`, keeping contracts consistent with the English-only product language.
