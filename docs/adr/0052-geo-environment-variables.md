# Geo environment variables

The Geo domain in Exito Tools uses `EXITO_GEO_TOKEN` for the provider token and `EXITO_GEO_BASE_URL` for the provider base URL. Endpoint paths remain in the Geo Domain API Client, while secrets stay in environment variables or non-committed dotenv files.
