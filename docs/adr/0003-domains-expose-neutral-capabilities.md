# Domains expose neutral capabilities

Operational domains in Exito Tools expose Use Cases and neutral capability metadata, while the CLI Surface adapts those capabilities into Commands and the TUI Surface adapts them into Actions. Domains must not depend on CLI or TUI details because the business capability should remain reusable across interaction surfaces.
