# YAML for non-sensitive configuration

Exito Tools will store non-sensitive application configuration in YAML, while secrets remain in environment variables and non-committed dotenv files. YAML is readable for humans, works well for profile/domain configuration, and keeps sensitive values out of committed configuration files.
