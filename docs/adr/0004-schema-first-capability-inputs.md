# Schema-first capability inputs

Exito Tools capabilities describe their required inputs as neutral schema metadata rather than duplicating input definitions in each interaction surface. This lets the CLI Surface validate flags and arguments, the TUI Surface build guided prompts or forms, and agents inspect what a capability needs without coupling those inputs to one UI.
