# Request ID and Correlation ID

Exito Tools distinguishes `requestId` from `correlationId`: `requestId` is generated by Exito Tools and unique per Capability Execution, while `correlationId` is optional and may be supplied by agents or users to group multiple executions. This preserves per-command debugging while still supporting higher-level automation correlation.
